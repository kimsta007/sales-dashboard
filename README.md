## Frontend

1. Run `npm install`
2. Run `npm start`

## Backend

1. Run `source ./venv/bin/activate` -- linux or mac
2. Run `python train_model.py`
3. Run `python app.py`

Open browser @ http://localhost:3000

